# Core (required)
anthropic>=0.45.0
httpx>=0.27.0

# Semantic matcher (matcher.py — local embeddings, no API cost at query time).
# Heavy install (pulls torch); everything else runs fine without it.
sentence-transformers>=3.0.0

# Agenda layer (agenda.py — RSS prominence capture; tolerant of feed quirks)
feedparser>=6.0.0

# Platform extractors (install what you need)
atproto>=0.0.50            # Bluesky
youtube-transcript-api>=1.0.0  # YouTube transcripts (no API key needed)

# Video transcription — TikTok/Instagram (optional)
# yt-dlp>=2024.0.0         # Video downloading
# openai>=1.0.0            # Whisper transcription

# Graph storage (optional)
# neo4j>=5.0.0
