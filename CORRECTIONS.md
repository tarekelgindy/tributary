# Corrections

A public, dated log of every substantive error found in Tributary's published output, and what was done about it. An empty section below is not a claim of perfection — it means no published-output errors have been *found yet*. The measured error rate (once published, per [METHODOLOGY.md](METHODOLOGY.md)) lives here too.

**Format:** date · output affected · the error · the fix · root cause.

---

## Published-output corrections

| Date | Output affected | Error | Fix | Root cause |
|------|-----------------|-------|-----|------------|
| 2026-06-10 | The entire public gallery (103 events) | Every gallery event link returned 404: the index was live but none of the event files had been deployed | `.gitignore`'s unanchored `events/` pattern also matched `gallery/events/`, so the files were silently never committed; pattern root-anchored, files deployed, live URLs verified | Unanchored ignore pattern — and a deployment check that verified the index but not the content it points to. Found by Tarek's manual QA. |
| 2026-06-10 | ~7.5% of carrier links across the corpus (274 of 3,667) | Carrier links labeled with a named source (e.g. "UN OCHA") pointed to encyclopedia pages *about the event* rather than the named carrier's own statement — the link did not support the attribution | Viewer now flags such links ("via Wikipedia") rather than letting the label imply first-party sourcing; generation prompt now requires the carrier's own URL or none ("a missing link is honest, a mislabeled one is not") | Web search surfaces aggregator pages; the model substituted them for primary links; URL verification checked existence, not that the page belongs to the named carrier. Found by Tarek's manual QA. Existing corpus entries retain the flag rather than being silently rewritten. |
| 2026-06-11 | Coverage-lean blocks across the gallery: 50 of 51 fuzzy "contains" outlet matches were wrong, affecting 83 published events — including **7 events whose "one-sided: no right-of-center coverage (possible blindspot)" claims were fabricated** | bias_db's fuzzy name matching equated different outlets by substring: *Iran International* → The Nation (left), *Israel National News* → The Nation (left), *Kyiv Independent* → The Independent (lean-left), *Oman/Zambian/Pakistan Observer* → The Observer NY (center), *National Post* → The Nation (left), *The Diplomat* → Diplomatic Courier, *The Insider* → Business Insider | Containment now counts as outlet identity only when the leftover text is a generic desk/edition suffix ("foxnews"⊂"foxnewsdigital") or a plural; everything else resolves to *unrated* (an honest gap). Corpus re-backfilled, gallery republished; the 7 blindspot claims downgraded to "insufficient rated coverage" | Substring matching mistaken for identity ("nation" ⊂ "nationalpost"). Found while auditing the new agenda layer's lean labels against a live capture (the P5 disprove-first pass), then measured corpus-wide before fixing. A false blindspot claim is the exact error class Gate 2 names as the project's most damaging — this one shipped through the lean pipeline instead, and the audit habit caught it. |
| 2026-07-05 | The chewing-gum example trace (a homepage-featured example): **37 of 40 attached Bluesky posts were unrelated to the narrative** (posts about TV shows, elections, dissertations — anything containing "seven years") | The social-spread search matched on the diagnostic n-gram "seven years", which is far too generic to identify this narrative; irrelevant posts were presented as observed spread | Posts pruned by a mechanical rule (only posts that mention gum remain — 3 of 40); prune documented in the trace's own search notes; pipeline fix queued: a relevance-confirmation stage before any social post attaches, and a generic-n-gram gate on social search terms | Generic n-grams treated as diagnostic; no second-stage relevance check on social matches (the same failure class Gate 1 found in embedding-only matching — topical/lexical overlap is not narrative identity). Found by Tarek's manual review of the homepage example. |
| 2026-07-05 | The same trace's earliest attestation (1993 Star Tribune article), shown with no indication that the reader's link goes to Snopes, not the article | Not a data error — the attestation is real and honestly recorded in the JSON ("cited as a primary source in Snopes' fact-check"; the 1993 print article is not online) — but the *presentation* hid the citation path, so a reader clicking the link landed on Snopes with no explanation and reasonably suspected fabrication | Viewer now labels such attestations "documented by Snopes/PolitiFact/…" on the milestone rail, the WHO strip, and the instance detail (which also explains the original is print/offline) | Secondhand-but-legitimate citations rendered identically to first-party links. Found by Tarek's manual review — the reader-trust failure occurred even though the underlying data was honest, which is its own lesson: provenance must be *visible*, not merely recorded. |
| 2026-07-05 | The chewing-gum trace, comprehensively (the full follow-up audit; Phase 2.8-C hand-repair, table-driven and asserted) | **(a)** A lineage composed mostly of fact-checks carried **zero `critic` roles** — Snopes/SciAm/McGill/Mayo/Healthline debunks were labeled amplifier or *institutional-adoption*, so the page displayed "Institutional adoption — McGill" for a debunking; **(b)** retrospective web content was dated to the period it *describes* (a June-2025 listicle dated "1950", a July-2024 newsletter dated "1970"); **(c)** the conceptual "origin" (dated 400 CE, attributed jointly to Hippocrates ~400 BCE and Dioscorides ~70 CE) asserted the **opposite** claim (mastic *aids* digestion); **(d)** 3 mutations had from==to endpoints and 2 were empty (mutation endpoints keyed by URL collapse when 5 entries share the Snopes URL); **(e)** one entry's URL pointed to a different paper than the one cited; **(f)** conceptual entries showed "✓ verified" when only the URL had been checked | 19 fact-check-function entries relabeled `critic`; retro entries re-dated to their real publication dates with a new `describes_period` field; the opposite-claim entry removed; mutations pruned to unambiguous ones (lexical 9→3, conceptual 22→8); the wrong-URL entry flagged `url-error` rather than guessed at; `cited_via` recorded on secondhand citations (empty = first-party); lineage status `single-origin` → `earliest-found`; viewer now reserves "✓ verified" for quote-found checks, renders no social amplifier roles, counts distinct documents, and filters degenerate mutations on ALL traces | One disease: interpretive AI labels rendered with structural confidence. The audit also verified what was *clean* — all six Snopes-bibliography print citations are genuine, the 1890 PMC scan is real — which matters for calibration. Pipeline-side fixes (role prompts, date semantics, index-keyed mutations, ancestor identity gate) are Phase 2.8-B; a blind human audit (Gate 2.8) decides when role labels count as trustworthy. Found by Tarek's review, then a systematic field-by-field audit. |

| 2026-07-05 | Every published fingerprint (35 across the store, examples, and gallery-embedded traces) — the Phase 2.8-D corpus sweep, measuring and fixing the label diseases the chewing-gum audit found | Corpus-wide counts: **23** examining-function sources (fact-checks, mythbusters, debunk columns) labeled amplifier/adopter; **28** extra "originator" labels beyond each lineage's earliest instance; **93** pre-1900 "idea ancestry" entries presented as lineage when they are unverifiable resemblance (17 of them labeled originator); **11** degenerate mutations; **25** "single-origin" status overclaims | All mechanically corrected in place with a visible note on every adjusted entry; idea lineages now anchor on living-memory origins (e.g. rigged-economy → Debs 1912; carrots → Mori 1904); the generation pipeline itself now enforces these rules, and the conceptual search is scoped to living memory by default (`--deep-history` opt-in). 137 legacy year-only dates shown as exact days remain flagged for regeneration | Interpretive AI labels rendered with structural confidence — one disease, many symptoms. These counts are the measured pre-fix label-error rate; Gate 2.8's blind human audit measures what remains. |

| 2026-07-09 | One gallery event (9577b8662adc, the killing of Hamas commander Mohammed Odeh) | `event_date` said **2024**-05-26; every dated carrier on the event is May **2026** (one dated 2026-05-26 exactly) — a two-year error in the event's headline date | Year corrected to 2026-05-26 in the source analysis and the live gallery copy, on the carrier-receipt evidence | Model-generated summary field never cross-checked against the carriers' own dates. Found while drafting Common Ground Report #1, whose editorial rule — date every quote from its receipt, never from a summary field — is now standing practice. |
| 2026-07-10 | Every shared `?load=` link on the live site — the reader's first impression of any trace or event | Opening a shared link showed a full-viewport "Tributary Trace Viewer" instruction page (drag a JSON file here) with the actual content rendered a full screen-height below it. Flagged by Tarek repeatedly; each earlier fix (hiding the drop zone sooner in the load path) had no visible effect | The `hidden` attribute was silently defeated by the element's own `display: flex` rule — any author `display` beats the UA stylesheet's `[hidden] {display:none}`. One CSS rule (`#drop-zone[hidden] {display:none}`) makes hidden mean hidden; verified in a real headless render | A CSS-vs-attribute interaction invisible to DOM-state checks: the attribute *was* set, so attribute-level validation (stub-DOM tests, DOM dumps) kept passing while every real render showed the instruction page. Lesson: visibility fixes must be asserted on pixels or computed style, never on attributes. |
| 2026-07-10 | The "vaccines do not cause autism" counter-trace (25a253481bed) and its claim-vs-rebuttal card — 2 of 15 attestation roles | **Mayo Clinic Health System labeled `critic` of the rebuttal it asserts verbatim** ("Vaccines do not cause autism" is the page's opening statement), and NPR's report on the CDC webpage change also labeled `critic` — on the card these rendered as pushback against the rebuttal | Mayo → `institutional-adoption`, NPR → `mention`, each with a dated correction note in the entry's own evidence field; card and published trace regenerated | The debunk-title→critic heuristic (added after the chewing-gum audit) is calibrated for **myth** traces, where a "myth debunked" page criticizes the traced claim — on a **rebuttal** trace the same page *asserts* the traced claim, so the heuristic inverts. Counter-traces are a new trace type (2026-07-10 pilot) and need role prompts aware of claim polarity; queued with the matcher's negation-blindness fix. Found by Tarek's review of the first vs-card. |
| 2026-07-10 | Every event page's "Trace this framing's origin" button, live site included — the advertised event→trace navigation | Clicking the button did nothing: the event layout replaces the whole `<main>` element, destroying the three tab panels a trace renders into, so the click died on a null panel. Broken silently since the event view began taking over `<main>` (the 2.5 presentation redesign); stub-DOM smoke tests could not catch it because their DOM stubs never return null | `render()` now restores the panel skeleton when missing; trace-click failures also now print an inline error instead of dying in the console, and `?load=` fetches use `cache: no-store` (a stale cached JSON produced the same "nothing happens" symptom during diagnosis). Regression test added that fails on the pre-fix code | A layout redesign changed an invariant (`<main>` always contains the panels) that another code path depended on, and no real-browser click test existed for the event→trace flow. Found by Tarek clicking through as a reader — the second time reader-testing caught what mechanical validation could not; a silent click failure also violated the "failures must say so" rule now applied to the handler. |

## Pipeline-level errors caught before/at publication

These shaped the methodology and are disclosed in the same spirit:

| Date | Component | Error | Fix | Root cause |
|------|-----------|-------|-----|------------|
| 2026-06-08 | Coverage-lean | Institutional actors (NGOs, courts, ministries) matched AllSides' think-tank/author entries and fabricated a left/right skew for several events | Distribution gated to news-media outlets only | Counting non-press actors as press coverage |
| 2026-06-08 | Coverage-lean | International events showed "one-sided" coverage that actually reflected unrated (non-US) outlets | Resolve-rate guard: below threshold, output says "insufficient rated coverage to judge skew" | US-centric ratings database mistaken for universal coverage |
| 2026-06-09 | Fingerprint viewer | A short-lived "Amplifiers" tab flattened spread-position roles into endorsement, presenting sources that *dispute* a claim as its champions | Tab removed; lineage view (which keeps critics distinct) remains | Treating interpretive AI role labels as stance; insufficient semantic validation before shipping |

*Root-cause pattern worth admitting: every entry above came from over-trusting an interpretive label or an incomplete database. The standing rule that resulted: prefer structural signals, audit AI labels before building on them, and say "insufficient data" instead of guessing.*
